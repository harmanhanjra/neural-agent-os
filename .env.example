# Agent OS — .env.example (no real keys — defensive security)
# Routing: omniroute / NVIDIA free / hermes/free / openrouter/free (Experiential removed)
# Key EXPLABS_API_KEY not used for routing; kept for reference only if gateway restored.
# Never commit .env; never expose tokens in source/config.
